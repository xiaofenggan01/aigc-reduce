# AIGC Reduce

降低学术论文 AIGC 查重率的 Claude Code Skill。

基于知网 3.0、万方、PaperPass 等主流检测器的技术原理，通过拆模板结构、打破均匀句式、嵌入操作细节、加入作者判断等方法，在保持学术语气的前提下让文本呈现「人类写作」的统计特征。

## 安装

```bash
# 克隆到 Claude Code skills 目录
git clone https://github.com/xiaofenggan01/aigc-reduce.git ~/.claude/skills/aigc-reduce
```

安装后，当你在 Claude Code 中提到「降 AI 味」「降 AIGC 率」「去 AI 检测」等关键词时，该 skill 会自动触发。

## 核心原理

主流 AIGC 检测器的核心判别维度是**统计特征的均匀性**，而非语义理解。AI 生成文本被识别，不是因为写得差，而是因为写得太「完美」——困惑度低、突发性低、句式均匀、段落长度一致、缺少作者判断。降重的本质是**打破这种均匀性**。

## 四层降重方法

### 第一层：表层改写

适用于 AI 生成痕迹较轻的段落。

- 拆长句为短句（50 字以上拆为 2-3 句）
- 被动改主动
- 同义词轮换
- 标点打破规律

### 第二层：结构重构

适用于检测报告中「高 AI 率」的段落。核心层。

- 拆模板化句式（嵌套编号、冒号并列、三段式万能结尾）
- 打破对称段长
- 加入段落间过渡

### 第三层：语义注入

适用于核心检测段落。效果最强。

- 嵌入操作观察与波动（意外发现、参数波动、异常值说明）
- 加入作者判断（主观判断句是人类写作的标志）
- 具体化模糊表述

### 第四层：专项针对

- 文献综述降 AI（避免并列列举，改为叙评结合）
- 结论与展望降 AI（避免对称列举，合并叙述）

## 自动化扫描

内置 `aigc_scan.py` 脚本，自动扫描文本的 7 个 AI 特征维度：

```bash
python3 scripts/aigc_scan.py your_paper.txt
```

输出报告包括：

| 维度 | 对应检测器 |
|------|-----------|
| 模板句式密度 | 知网语义指纹 |
| 突发性 CV | GPTZero 核心算法 |
| 段落对称性 | 知网格式规范性校验 |
| 嵌套编号 | 统计检测（结构模板化） |
| 冒号并列 | 知网逻辑断层检测 |
| 被动语态 | Turnitin 句式特征分析 |
| 标点规律 | 万方 BERT 语义分析 |

支持 `--json` 输出结构化数据：

```bash
python3 scripts/aigc_scan.py your_paper.txt --json
```

## 文件结构

```
aigc-reduce/
├── SKILL.md                      # Skill 定义（触发条件 + 方法体系 + 工作流程）
├── README.md                     # 本文件
├── references/
│   └── detection-principles.md   # 主流检测器技术原理与弱点
└── scripts/
    └── aigc_scan.py              # 自动化 AI 特征扫描脚本
```

## 检测器参考数据

| 产品 | 综合准确率 | 假阳性率 | 核心检测维度 |
|------|-----------|---------|-------------|
| 知网 3.0 | 98.6% | 1.2% | 语义指纹、格式规范性 |
| 万方 | ~95% | ~3% | 多模型融合 |
| PaperPass | ~97% | 0.3% | 语言风格一致性 |

## License

MIT
